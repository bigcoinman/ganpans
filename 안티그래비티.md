---
title: 안티그래비티 코딩 지침
description: 코드를 작성하기 전, 작성하는 동안, 작성한 후에 지켜야 할 4가지 핵심 원칙
alwaysApply: true
---

# 안티그래비티 코딩 지침

> 이 문서는 코딩 작업을 할 때 항상 자동으로 적용되는 규칙이에요.

---

## 핵심 원칙 4가지

1. **먼저 생각하기** — 코드부터 쓰지 말고, 애매한 부분은 먼저 물어본다.
2. **단순하게 만들기** — 필요한 만큼만 만든다. 미리 복잡하게 만들지 않는다.
3. **수술하듯 고치기** — 요청받은 부분만 정확히 고친다. 다른 곳은 건드리지 않는다.
4. **목표를 확인하며 진행하기** — "다 했어요"가 아니라 "이렇게 확인하면 됩니다"까지 제시한다.

---

### 1. 먼저 생각하기 (Think Before Coding)
**규칙**: 요청이 애매하거나 여러 가지로 해석될 수 있으면, 코드를 쓰기 전에 반드시 먼저 확인한다.

- **하지 말 것**:
  - 파일 형식, 저장 위치, 포함할 항목(필드)을 마음대로 추측해서 정하기
  - "빠르게 해줘", "고쳐줘" 같은 애매한 표현을 임의로 하나의 뜻으로 해석해서 바로 구현하기
- **할 것**:
  - 가능한 해석이 여러 개면 목록으로 정리해서 보여주고 선택하게 한다
  - 각 선택지마다 예상 작업 범위(간단히)를 함께 제시한다
  - 정말 간단하고 명확한 요청이면 바로 진행해도 된다 (사소한 것까지 매번 물어보지 않기)

---

### 2. 단순하게 만들기 (Simplicity First)
**규칙**: 지금 필요한 것만 정확히 만든다. "나중에 필요할 수도 있으니까"로 미리 만들지 않는다.

- **하지 말 것**:
  - 요청받지 않은 옵션, 설정값, 확장 포인트를 미리 추가하기
  - 디자인 패턴(전략 패턴, 추상 클래스 등)을 실제로 여러 종류가 필요하지도 않은데 먼저 도입하기
  - 캐싱, 검증, 알림 같은 "있으면 좋은" 기능을 요청 없이 끼워 넣기
- **할 것**:
  - 가장 짧고 직관적인 구현부터 시작한다
  - 확장이 필요해지면 "그때" 리팩토링한다는 걸 원칙으로 한다
  - 복잡한 기능이 진짜 필요하다고 판단되면, 미리 만들지 말고 사용자에게 먼저 제안한다
- **체크 질문**: *"이 옵션/기능을 지금 당장 쓰는 곳이 있는가?"* → 없으면 빼기

---

### 3. 수술하듯 고치기 (Surgical Changes)
**규칙**: 요청받은 버그/기능과 직접 관련된 줄만 수정한다. 그 외에는 손대지 않는다.

- **하지 말 것**:
  - 버그를 고치면서 스타일(따옴표, 들여쓰기, 네이밍)을 임의로 바꾸기
  - 요청하지 않은 타입 힌트, docstring, 주석을 추가하기
  - 관련 없는 코드를 "더 좋아 보여서" 리팩토링하기
  - 요청받지 않은 검증 로직이나 예외 처리를 추가하기
- **할 것**:
  - diff(변경 사항)를 최소화한다 — 딱 문제를 해결하는 줄만 바꾼다
  - 기존 코드의 스타일(따옴표 종류, 들여쓰기, 네이밍 방식)을 그대로 따른다
- **체크 질문**: *"이 줄을 안 바꿔도 요청한 문제가 해결되는가?"* → 그렇다면 바꾸지 않기

---

### 4. 목표를 확인하며 진행하기 (Goal-Driven Execution)
**규칙**: 애매한 계획으로 바로 실행하지 않는다. 확인 가능한(verifiable) 단계로 나누고, 각 단계마다 "어떻게 확인할지"를 함께 제시한다.

- **하지 말 것**:
  - "검토하고, 문제를 찾고, 개선하고, 테스트하겠다" 같은 애매한 계획만 말하고 바로 진행하기
  - 버그를 재현하지도 않고 바로 코드를 고치기
  - 큰 기능을 한 번에 통째로 구현하기
- **할 것**:
  - 큰 작업은 여러 단계로 나눈다
  - 각 단계마다 "확인 방법"을 구체적으로 제시한다 (테스트, 수동 확인 방법 등)
  - 버그를 고칠 때는 먼저 버그를 재현하는 테스트를 작성한 뒤 고친다
  - 목표(성공 기준)가 불명확하면, 코드를 짜기 전에 먼저 성공 기준을 물어본다

```markdown
1단계: [무엇을 할지]
   확인 방법: [테스트/수동 확인 방법]
2단계: [무엇을 할지]
   확인 방법: [테스트/수동 확인 방법]
```

---

### 흔한 실수 요약표

| 원칙 | 흔한 실수 | 올바른 대처 |
| :--- | :--- | :--- |
| **먼저 생각하기** | 형식/범위/항목을 마음대로 추측 | 가정을 명확히 밝히고 먼저 확인 |
| **단순하게 만들기** | 단순한 계산에 디자인 패턴 도입 | 실제로 필요해질 때까지 함수 하나로 유지 |
| **수술하듯 고치기** | 버그 고치며 스타일/타입힌트까지 변경 | 문제 해결에 필요한 줄만 수정 |
| **목표 확인하며 진행** | "검토하고 개선하겠다"는 애매한 계획 | "재현 테스트 작성 → 수정 → 회귀 확인" 형식으로 진행 |

---

> **한 줄 요약**: 좋은 코드는 오늘의 문제를 단순하게 푸는 코드다. 내일 생길지도 모르는 문제를 미리 걱정해서 복잡하게 만든 코드가 아니다.
