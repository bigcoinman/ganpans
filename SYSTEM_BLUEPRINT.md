# [간판지원단 단일 절대 설계도 (SYSTEM BLUEPRINT)]

> **헌법 원칙**: 간판지원단은 오직 `index.html` 단 하나의 '단일 반응형 웹(Single Responsive Web)'으로 통폐합된 단일 사이트이며, 모든 데이터는 수파베이스 `applications` 및 `users` 단 1개의 데이터 원천(SSOT)을 100% 절대 추종한다. 어떠한 독자 캐시, 독자 테이블, 이원화된 로직도 영구 엄격 금지한다.

---

## 1. 단일 진실의 원천 (SSOT) 데이터 아키텍처

```
                  ┌───────────────────────────────┐
                  │    Supabase Cloud Database    │
                  │   - users (14명 명부)         │
                  │   - applications (신청/물건)  │
                  │   - inquiries (문의)          │
                  │   - site_stats (방문자 통계)  │
                  └───────────────┬───────────────┘
                                  │ (WebSocket Realtime)
                                  ▼
                  ┌───────────────────────────────┐
                  │    DataStore.js (단일 저장소) │
                  │  - getApplications()          │
                  │  - getUsers()                 │
                  │  - getBizItemsForUser()       │
                  └───────────────┬───────────────┘
                                  │ (0초 실시간 동기화)
         ┌────────────────────────┼────────────────────────┬────────────────────────┐
         ▼                        ▼                        ▼                        ▼
[1. 최고관리자 화면]     [2. 영업자 화면]         [3. 시공사 화면]         [4. 일반 점주 화면]
- 전체 회원/권한 관리    - 상단: 온라인간편신청    - 배정된 시공건 조회     - 내 신청서 확인
- 신청서 승인/반려       - 하단: 영업물건진행      - 시안 등록 (1~5장)      - 현장사진 등록
- 영업물건 등록/시공배정 - 시안/사진 원스톱 확인  - 완료사진/계산서 증빙   - 시안 검토 및 승인
```

---

## 2. 4대 권한별 역할 및 화면 헌법 (Role Blueprint)

### [권한 1] 최고관리자 (`admin`) — 단일 진실의 절대 기준점
* **화면 6대 표준 메뉴**:
  1. `회원 정보 관리` (전체 가입 회원 명부 및 권한 전환)
  2. `신청서 목록` (온라인 간편 지원 신청 접수건 관리 - 심사 상태, 담당 영업자 지정, 영업물건 승인)
  3. `영업물건 진행상황` (공단 접수 영업물건 관리 및 시공사 배정)
  4. `시공업체 진행현황` (전체 시공 배정건 공정 및 정산 관리)
  5. `3초 간편 문의 목록` (간편 상담 문의 접수)
  6. `팝업창 관리` (메인 공지 팝업)
* **절대 원칙**:
  - 최고관리자 화면에 없는 건은 그 어떤 하위 화면(영업자/시공사)에도 절대 존재할 수 없다 (부존재 일치 의무).
  - 최고관리자가 담당 영업자나 시공사를 변경하면 0초 만에 해당 사용자 화면으로 즉시 연동된다.

---

### [권한 2] 영업자 (`business`) — 접수 창구와 정식 공정관리의 완전 분리
* **영역 A. 상단 [내 온라인 간편 지원 신청 내역] (초기 접수 창구)**:
  - **노출 대상**: 내 영업자 코드(`referrerCode`)로 인입된 모든 초기 신청서.
  - **상태**: `서류준비 & 접수대기`, `심사대기` 등 초기 접수 상태.
  - **기능**: 현장사진 촬영/등록(5장), 접수 상태 확인.
  - **승인 권한 제한**: 점주 전용 버튼인 `[시안 승인 / 마음에 듭니다]`는 영업자에게 노출되지 않으며, `점주 시안 검토 중` 상태 뱃지만 노출하여 혼선을 차단한다.
* **영역 B. 하단 [내 영업물건 현황 및 진행상황] (정식 프로젝트 공정관리)**:
  - **노출 대상**: 최고관리자가 `영업물건으로 변경(isBizItem: true)` 승인한 물건만 노출.
  - **상태**: `접수완료`, `대상자선정`, `간판시공 준비중`, `간판시공완료`.
  - **핵심 탑재 기능**:
    1. **현장사진 확인**: `applications` 원본과 매칭하여 실제 등록된 사진(예: 5장) 즉시 확인.
    2. **간판 디자인 시안 확인**: 시공사가 등록한 시안(1~5장) 확인, `[시안 크게보기]` 공용 모달 호출, `점주 시안 검토중/확정완료` 실시간 모니터링.
    3. **실시간 공정 확인**: 공단 접수상태 및 진행상황 실시간 추종.

---

### [권한 3] 시공사 (`constructor`) — 현장 시공 및 증빙 책임자
* **노출 대상**: 최고관리자가 본인 시공사 코드(`constCode`)로 배정한 물건만 노출.
* **핵심 기능**:
  1. **간판 디자인 시안 업로드**: 최대 5장, 300KB 이하 강제 자동 압축 등록.
  2. **시안 승인 여부 실시간 확인**: 점주가 승인했는지 여부를 실시간 확인 후 제작 착수.
  3. **시공 완료 사진(최대 5장) 및 세금계산서 업로드**: 공정 완료 증빙.

---

### [권한 4] 일반 점주 (`normal`) — 간판의 최종 주인 및 승인 권한자
* **화면**: `[내 온라인 간편 지원 신청 내역]` 단일 화면.
* **핵심 권한**:
  1. **신청서 작성 및 현장사진 등록**: 간판 신청 및 현장사진 5장 업로드.
  2. **간판 디자인 시안 최종 승인 주체**: 시공사가 올린 시안을 크게보고, 마음에 들면 `[시안 승인 / 마음에 듭니다]` 녹색 버튼을 직접 눌러 시공을 확정하는 **유일한 최종 승인 권한자**.

---

## 3. 이원화 절대 금지 원칙 (Zero Dual-Architecture)

1. **단일 데이터 참조 의무**:
   - 어떤 화면이든 `applications` 레코드의 원본 필드(`signDraftPhotos`, `draftStatus`, `photos`, `image_url`)를 직접 읽어야 하며, 화면별로 독자적인 사본 객체를 만들어 보관하는 것을 영구 금지한다.
2. **단일 모달/함수 호출 의무**:
   - 시안 크게보기: `window.viewDraftModal(appId)` 단일 함수만 사용한다.
   - 사진 확인/다운로드: `window.downloadApplicationPhotos(appId)` 단일 함수만 사용한다.
3. **독자 팝업/독자 상태 분기 영구 금지**:
   - 특정 역할을 위한 임시 땜빵용 함수(예: `viewDraftModalForSales`)를 새로 만드는 행위를 영구 금지한다.

---

## 4. 설계도 절대 보존 및 변경 절차 (Preservation Law)

* 본 설계도에 정의된 4대 권한 화면 및 데이터 흐름은 **간판지원단의 불변의 헌법**이다.
* 향후 어떠한 신규 기능(알림, 엑셀, 통계 등)을 추가하거나 버그를 수정하더라도, **본 설계도에 정의된 기존 기능은 단 1%도 훼손되어서는 안 된다.**
* 코드를 수정하기 전 반드시 본 설계도를 대조하고, 수정 후에는 자동 검문소(`npm run verify`)를 통과해야만 배포할 수 있다.
