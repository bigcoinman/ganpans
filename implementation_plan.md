# 관리자 대시보드 제어 콘솔 구현 계획 (Implementation Plan)

본 계획서는 일반 회원 및 영업자 회원에게 노출되던 기존 매니저 콘솔을 숨기고, 별도의 **관리자 계정(`admin`)**으로 로그인했을 때만 노출되도록 하는 **관리자 전용 대시보드 및 권한 제어 기능** 구현 계획입니다.

## User Review Required

> [!IMPORTANT]
> 1. **관리자 기본 계정 정의**:
>    * 아이디: `admin`
>    * 비밀번호: `admin123!`
>    * 이름: `최고관리자`
>    * 권한 역할(`role`): `admin`
> 2. **접근 제어 및 보안**:
>    * 일반 회원(`testuser`) 및 영업자 회원(`bizuser`) 로그인 시 기존의 테스트 조작 콘솔(`.manager-admin-panel`)을 완전히 보이지 않도록 숨깁니다 (`display: none;`).
>    * 오직 `admin` 계정으로 로그인했을 때만 화면에 노출되며, 이때는 기존 일반 회원의 대시보드 뷰(`.dashboard-grid`)를 숨기고 대시보드 본문에 관리자 콘솔만 넓고 깔끔하게 배치합니다.
> 3. **관리자 전용 UI 튜닝**:
>    * 관리자 모드일 때는 접기/펼치기 토글 기능을 비활성화하고, 점선 테두리(Dashed border)가 아닌 단단한 솔리드 테두리와 전용 타이틀("간판지원단 관리자 대시보드 콘솔")을 적용하여 정식 관리 툴처럼 변경합니다.

---

## Proposed Changes

### 1. 사용자 계정 데이터베이스 수정

#### [MODIFY] [script.js](file:///c:/Users/BINY/.vscode-shared/간판지원단/script.js)
* **기본 사용자 목록 수정**: `defaultUsers`에 `admin` 계정 정보 추가.
* **하위 호환 검사**: 이미 로컬 스토리지에 데이터가 적재되어 있을 때도 `admin` 아이디가 없으면 자동으로 푸시하도록 동적 로직 보완.
* **헤더 표시 튜닝**: 로그인 정보 표시 시 관리자 권한 여부에 따라 "홍길동님 (관리자)" 형태로 깔끔하게 표시.

---

### 2. 대시보드 권한 기반 렌더링 수정

#### [MODIFY] [dashboard.js](file:///c:/Users/BINY/.vscode-shared/간판지원단/dashboard.js)
* **`updateSessionUI()` 수정**:
  * 로그인된 유저의 `role`이 `admin`인지 검사.
  * `admin`인 경우:
    * 일반 회원용 그리드(`.dashboard-grid`)와 영업자 전환 신청 영역(`#conversion-area`)을 숨김 (`display: none`).
    * 관리자 패널(`.manager-admin-panel`)의 접기/펼치기 클릭 이벤트를 비활성화하고 본문을 항상 노출시킴.
    * 헤더 텍스트를 "관리자 대시보드 콘솔"로 변경하고 프리미엄 스타일(테두리, 배경) 인라인 튜닝.
  * `admin`이 아닌 경우:
    * 기존 대시보드 그리드 노출.
    * 관리자 패널(`.manager-admin-panel`)을 숨김 (`display: none`).

---

## Verification Plan

### Manual Verification
1. **일반 회원 테스트**: `testuser` / `test123!` 로그인 후 대시보드(`dashboard.html`) 접속 시, 하단에 기존에 보이던 "간판지원단 매니저 승인 관리 콘솔"이 전혀 노출되지 않는지 확인.
2. **영업자 회원 테스트**: `bizuser` / `biz123!` 로그인 시에도 관리 콘솔이 전혀 노출되지 않는지 확인.
3. **관리자 계정 테스트**: `admin` / `admin123!` 로그인 테스트.
   * 로그인 후 대시보드 접속 시 일반 회원 대시보드 대신 **관리자 전용 제어 콘솔**이 메인에 로드되는지 확인.
   * 팝업창 등록/수정/삭제 및 활성화 제어 테스트.
   * 영업자 승인 요청 처리(승인/반려) 및 영업 물건 상태 변경 테스트.
