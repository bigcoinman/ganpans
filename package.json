{
  "name": "ganpan-support-mobile",
  "version": "1.0.0",
  "description": "간판지원단 모바일 앱 빌드 및 패키징 설정",
  "main": "index.html",
  "scripts": {
    "build": "echo 'Build complete'",
    "deploy": "npx wrangler deploy",
    "package": "powershell -Command \"Compress-Archive -Path index.html, style.css, app.css, supabase-config.js, data-store.js, security-utils.js, kakao-notify.js, app.js, manifest.json, sw.js, '*간판*.png', '*이미지*.png', '*로고*.png' -DestinationPath 간판지원단_단일웹.zip -Force\"",
    "cap:init": "npx cap init 간판지원단 com.ganpan.support --web-dir=.",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}
