# 간판지원단 모바일 앱 설치 및 APK 빌드 가이드

이 문서는 **간판지원단** 웹 애플리케이션을 모바일 기기에 설치하고 실행하기 위한 두 가지 방법(PWA 설치 및 Capacitor를 이용한 APK 빌드)을 안내합니다.

---

## 방법 1. 모바일 브라우저 PWA로 간편 설치 (추천 ⭐⭐⭐)

PWA(Progressive Web App)는 모바일 브라우저를 통해 마켓(App Store/Play Store) 설치 없이 홈 화면에 즉시 설치하여 사용하는 모바일 웹 앱 표준 기술입니다.

### 📲 QR 코드로 모바일에서 접속하여 설치하기
1. PC나 태블릿에서 **간판지원단** 웹사이트에 접속합니다.
2. 헤더 내비게이션 바 우측의 **"앱 설치 <i class='fa-solid fa-mobile-screen-button'></i>"** 버튼을 클릭합니다.
3. 화면에 나타나는 **QR 코드**를 스마트폰 카메라로 스캔합니다.
4. 연결된 웹 브라우저(Chrome/Safari 등)에서 아래의 운영체제별 가이드에 따라 설치합니다.

---

### 🤖 안드로이드 (Android - Chrome 기준)
1. QR 코드를 스캔하거나 모바일 크롬으로 사이트에 접속합니다.
2. 화면 하단에 뜨는 **"홈 화면에 간판지원단 추가"** 또는 **"앱 설치"** 배너를 탭합니다.
3. 배너가 뜨지 않을 경우, 주소창 우측의 **더보기 메뉴(점 3개 `⋮`)**를 누르고 **"앱 설치"** 또는 **"홈 화면에 추가"**를 선택합니다.
4. 설치가 완료되면 바탕화면에 앱 아이콘이 생성되며, 실행 시 상단 주소창이 없는 전체 화면 앱으로 실행됩니다.

### 🍎 아이폰 (iOS - Safari 기준)
1. QR 코드를 스캔하거나 사파리(Safari) 브라우저로 사이트에 접속합니다.
2. 하단 툴바의 **공유 버튼(네모 안의 위쪽 화살표 `[↑]`)**을 탭합니다.
3. 메뉴를 아래로 스크롤하여 **"홈 화면에 추가"**를 선택합니다.
4. 우측 상단의 **"추가"**를 클릭하면 홈 화면에 앱 아이콘이 생성되고, 독립된 앱처럼 작동합니다.

---

## 방법 2. Capacitor를 이용하여 네이티브 안드로이드 앱(APK) 빌드하기

실제 모바일 배포용 안드로이드 설치 파일(`.apk`)을 컴퓨터에서 직접 빌드하고 싶다면 아래 과정을 진행합니다.

### 🛠️ 사전 준비 사항
1. **Node.js**: v18 이상 (설치 완료됨)
2. **Java JDK**: JDK 17 이상 권장
3. **Android Studio**: Android SDK 및 Gradle 빌드 도구 포함 설치 필수

### 🚀 빌드 단계

#### 1. 의존성 패키지 설치
터미널(PowerShell 등)을 열고 프로젝트 폴더 경로에서 다음 명령을 실행합니다:
```bash
npm install
```

#### 2. Capacitor Android 플랫폼 추가
```bash
npx cap add android
```
*(참고: Android 프로젝트 뼈대가 `android/` 폴더에 생성됩니다.)*

#### 3. 웹 소스 코드 동기화
현재 폴더의 HTML/CSS/JS 파일을 Capacitor 안드로이드 빌드 경로로 동기화합니다:
```bash
npx cap sync
```

#### 4. 안드로이드 스튜디오에서 프로젝트 열기 및 APK 빌드
```bash
npx cap open android
```
* 명령어가 실행되면 안드로이드 스튜디오(Android Studio)가 자동으로 켜지고 프로젝트가 로드됩니다.
* 안드로이드 스튜디오 상단 메뉴에서 **Build > Build Bundle(s) / APK(s) > Build APK(s)**를 클릭합니다.
* 빌드가 완료되면 우측 하단 팝업의 **"locate"** 링크를 눌러 생성된 `app-debug.apk` 파일을 획득할 수 있습니다.
* 이 `.apk` 파일을 스마트폰으로 전송하여 직접 설치할 수 있습니다.

---

## 📦 모바일 소스 코드 압축(ZIP) 다운로드
오프라인에서 이 앱을 직접 폴더째 모바일 등으로 복사하여 사용하고 싶다면, 터미널에서 다음 스크립트를 실행해 다운로드용 압축 파일을 만들 수 있습니다.

```bash
npm run package
```
* 실행하면 루트 폴더에 `간판지원단_모바일앱.zip` 파일이 생성됩니다.
